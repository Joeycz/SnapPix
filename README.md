<p align="center">
  <img src="https://raw.githubusercontent.com/Joeycz/SnapPix/main/icons/logo.png" width="80%" alt="SnapPix Logo">
</p>

<h1 align="center">SnapPix</h1>

<p align="center">
  <strong>快速截取网页任意元素的 Chrome 扩展</strong>
</p>

<p align="center">
  <a href="#功能特点">功能</a> •
  <a href="#安装方法">安装</a> •
  <a href="#使用方法">使用</a> •
  <a href="#技术栈">技术</a> •
  <a href="#许可证">许可证</a>
</p>

---

## 功能特点

- 🎯 **鼠标选择模式** - 悬停高亮显示元素，点击即可截图
- 🔍 **CSS 选择器模式** - 输入选择器直接定位元素截图
- 📸 支持截取**不可见区域**的 DOM 节点
- 🖼️ 支持 PNG 和 JPEG 格式
- ⚙️ 可调节图片质量
- ⌨️ 按 ESC 取消选择

## 安装方法

### 从源码安装

1. 克隆仓库
   ```bash
   git clone https://github.com/your-username/snappix.git
   ```

2. 打开 Chrome 浏览器，访问 `chrome://extensions/`

3. 开启右上角的「开发者模式」

4. 点击「加载已解压的扩展程序」

5. 选择 `snappix` 文件夹

## 使用方法

### 方式一：鼠标选择

1. 点击浏览器工具栏中的 SnapPix 图标
2. 选择图片格式（PNG/JPEG）和质量
3. 点击「开始选择节点」按钮
4. 在网页上移动鼠标，元素会高亮显示
5. 点击要截图的元素
6. 截图会自动保存到下载文件夹

### 方式二：CSS 选择器

1. 点击插件图标，切换到「选择器」标签
2. 输入 CSS 选择器，例如：
   - `#header` - ID 选择器
   - `.card` - 类选择器
   - `div.content` - 元素+类选择器
   - `[data-id="123"]` - 属性选择器
   - `.list > li:first-child` - 复杂选择器
3. 点击「查找并截图」
4. 插件会自动定位元素并截图（即使元素不在可视区域）

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `ESC` | 取消选择模式 |

## 技术栈

- Chrome Extension Manifest V3
- **[snapdom](https://github.com/zumerlab/snapdom)** - 高性能 DOM 截图引擎
- Vanilla JavaScript

### 为什么选择 snapdom？

| 特性 | snapdom | html2canvas |
|------|---------|-------------|
| 体积 | ~96KB | ~200KB |
| 简单元素截图 | **0.5ms** | 67ms |
| 复杂元素截图 | **17ms** | 178ms |
| 不可见区域 | ✅ 支持 | ✅ 支持 |
| CSS 还原度 | 极高 | 一般 |

## 文件结构

```
snappix/
├── manifest.json          # 扩展配置
├── popup/                 # 弹出窗口
│   ├── popup.html
│   ├── popup.css
│   └── popup.js
├── content/               # 内容脚本
│   ├── content.js
│   └── content.css
├── background/            # 后台服务
│   └── service-worker.js
├── lib/                   # 第三方库
│   └── snapdom.min.js
└── icons/                 # 图标
    ├── icon16.png
    ├── icon48.png
    ├── icon128.png
    └── logo.png
```

## 注意事项

- 无法在 Chrome 内置页面（`chrome://`）使用
- 跨域图片需要 CORS 支持
- 支持 Shadow DOM、伪元素、CSS 变量等高级特性

## 贡献

欢迎提交 Issue 和 Pull Request！

## 许可证

[MIT](LICENSE)

---

<p align="center">
  Made with ❤️ by <a href="https://github.com/Joeycz">Milo</a>
</p>
